extends layout

block content
    form(method="post")
        label: p 标题:
            input(name="title" type="title")
        br
        label: p 正文：
            textarea(name="content", cols="100", rows="20")
        br
        input(type="submit" value="发表")
